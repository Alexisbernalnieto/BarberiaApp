// COPIA Y PEGA ESTO EN: Firebase Console -> Firestore Database -> Reglas (Rules)

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Regla temporal para DESARROLLO
    // Permite que CUALQUIERA lea y escriba (necesario porque las cuentas "default" no son usuarios reales de Firebase)
    match /{document=**} {
      allow read, write: if true;
    }
    
  }
}
